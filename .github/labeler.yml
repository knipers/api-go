# Add 'feature' label to any PR where the head branch name starts with `feature` or has a `feature` section in the name
feature:
 - head-branch: ['^feature', 'feature']
 - head-branch: ['^enhancement', 'feature']

fix:
  - head-branch: ['^fix', 'fix']

hotfix:
  - head-branch: ['^hotfix', 'hotfix']

   
